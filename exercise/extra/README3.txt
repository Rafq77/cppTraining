Exercise "Move"

 - Provide move semantics for Group.
   (Tests require that a moved from object is valid enough to be moved
   again without undefined behaviour.)

 - Provide move semantics for Canvas.

 - Look where else move semantics makes sense.

 - Provide move semantics for XWin (and helpers).
